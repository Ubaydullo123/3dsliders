const swiper = new Swiper(".swiper-cotainer", {
  effect: "cube",
  grabCursor: true,
  cubeEffect: {
    shadow: true,
    sliderShadows: true,
    shadowEffect: 20,
    shadowScale: 0.94,
  },
});
